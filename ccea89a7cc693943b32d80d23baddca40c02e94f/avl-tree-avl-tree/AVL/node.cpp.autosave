#include "node.h"

Node::Node(int )
{
       node->key   = key;
       node->left   = NULL;
       node->right  = NULL;
       node->height = 1;  // new node is initially added at leaf
}
Node::Node(int )
{
       node->key   = key;
       node->left   = NULL;
       node->right  = NULL;
       node->height = 1;  // new node is initially added at leaf
}
